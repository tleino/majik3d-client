To create necessary files:

$ automake -a
$ aclocal
$ autoconf

To create suitable Makefiles (recommended to do always after cvs checkout):

$ ./configure

To compile:

$ make

NEVER EVER cvs add files such as Makefile.am, Makefile.in, configure or
such!
